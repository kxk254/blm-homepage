# backendはDockerコンテナではなくホスト上でuvicornが常駐している(systemd管理)。
# frontendをDockerコンテナで動かす場合は、コンテナからホストへ抜けるための特別なDNS名
# host.docker.internal を指定する(docker-compose.ymlでextra_hosts設定済み)。
# ローカルでNext.jsだけを`npm run dev`する場合は http://localhost:8000 にする
BACKEND_INTERNAL_URL=http://host.docker.internal:8000
