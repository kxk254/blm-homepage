# イヤリング・小物販売ECサイト
## 管理画面「ユーザー・権限管理」実装指示書

---

## 0. 実装ステータス（現在）

**現時点では未実装。運用は`customers.is_admin`（true/falseの単純な管理者フラグ）のまま継続する。**

理由：現在は一人運営であり、この規模の権限システムを今すぐ作るとオーバースペックになるため。

このドキュメントは、**実際にスタッフ・マネージャーを雇用するタイミングで実装に着手するための設計資料**として保管する。着手時は本ドキュメント一式（特に末尾の「28. 実装時の申し送り事項」）をそのまま実装指示として渡せばよい。

---

## 1. 目的

本ECサイトでは、イヤリング・アクセサリー・小物等の商品販売を行う。

現在は一人で運営するが、将来的にスタッフやマネージャーを追加する可能性があるため、管理画面にはユーザーごとの権限管理機能を実装する。

ただし、一人運営の段階から過剰に複雑な権限システムにはせず、

**「必要な操作だけをユーザーごとに許可できる、シンプルで拡張可能な仕組み」**

とする。

---

## 2. 基本的な考え方

権限は「画面を見ることができるか」だけではなく、**何の操作ができるか**で設定する。

例えば商品管理の場合、

- 商品を見る
- 商品を追加する
- 商品情報を編集する
- 商品画像を変更する
- 商品を削除する

をそれぞれ別の権限として扱う。

これにより、

「商品登録はできるが、商品削除はできない」

「注文処理はできるが、商品の価格は変更できない」

といった運用を可能にする。

---

## 3. ロール

ロールは以下の3種類とする。

### 管理者（admin）

- 全権限を持つ。
- 個別の権限ON/OFF設定は行わない。
- 常に全権限を許可する。
- 管理者自身の権限を誤って制限できない構造とする。

### マネージャー（manager）

- 権限を個別設定できる。
- マネージャー用の初期権限セットを設定する。
- 管理者が必要に応じて権限を変更できる。

### スタッフ（staff）

- 権限を個別設定できる。
- スタッフ用の初期権限セットを設定する。
- 管理者が必要に応じて権限を変更できる。

マネージャーとスタッフは、基本的には同じPermissionの仕組みを使用する。

違いは主に初期権限セットとする。

---

## 4. 商品管理の権限

イヤリング・アクセサリー・小物の商品販売を想定し、以下に分ける。

### `product.view`

商品を見る。

- 商品一覧
- 商品詳細
- 商品情報の確認

### `product.create`

商品を追加する。

- 新商品登録
- 商品基本情報の登録

### `product.edit`

商品情報を編集する。

対象例：

- 商品名
- 商品説明
- 価格
- 在庫
- カテゴリー
- 素材
- サイズ
- カラー
- 販売状態
- その他の商品情報

### `product.image`

商品画像を操作する。

- 画像アップロード
- 画像変更
- 画像削除

### `product.delete`

商品を削除する。

---

## 5. 商品管理UIについて

現在の管理画面では、**商品編集＋画像アップロード**が一体になっているため、無理に画面を分離する必要はない。

ただし、バックエンド上のPermissionは分離する。

例えば、

```text
product.edit = ON
product.image = OFF
```

の場合、商品編集画面にはアクセスできるが、画像アップロード・変更・削除は実行できないようにする。

UI上でも、権限がない場合は該当ボタンを非表示または無効化する。

ただし、最終的な権限判定はFastAPI側で必ず行う。

---

## 6. 注文管理

ECサイト運営上、注文処理は重要な機能なので以下に分ける。

### `order.view`

注文を見る。

- 注文一覧
- 注文詳細
- 注文者情報
- 注文商品
- 金額等の確認

### `order.status_update`

注文ステータスを変更する。

例：

- 未処理
- 支払済み
- 発送準備中
- 発送済み
- 完了
- キャンセル

実際のステータスは現在のシステム仕様に合わせる。

### `order.delete`

注文を削除する。

ただし、注文データは販売履歴として重要なため、**物理削除は慎重に扱う**。

可能であれば、

**キャンセル／無効化／アーカイブ**

などの論理的な処理を優先する。

物理削除を実装する場合は、管理者のみ許可する運用も検討する。

---

## 7. 顧客管理

現在の実装では顧客情報は一覧表示が中心で、編集・削除機能は存在しない。

そのため、現時点では以下のみとする。

### `customer.view`

顧客情報を見る。

- 顧客一覧
- 顧客詳細
- 購入履歴等、現在実装されている情報

現時点では、

- `customer.edit`
- `customer.delete`

は実装しない。

将来、顧客情報編集・削除機能を追加する場合に追加する。

---

## 8. テーマ・サイト管理

サイトのデザイン・テーマに関する権限。

### `theme.view`

テーマ・サイト設定を見る。

### `theme.create`

テーマを作成する。

### `theme.edit`

テーマを編集する。

### `theme.delete`

テーマを削除する。

イヤリング・小物販売サイトの運営を考えると、テーマ変更は商品登録などの日常業務とは異なるため、基本的には管理者・マネージャー向けの権限とする。

---

## 9. 権限一覧

| 分類 | Permission | 内容 |
|---|---|---|
| 商品 | product.view | 商品閲覧 |
| 商品 | product.create | 商品追加 |
| 商品 | product.edit | 商品情報編集 |
| 商品 | product.image | 商品画像操作 |
| 商品 | product.delete | 商品削除 |
| 注文 | order.view | 注文閲覧 |
| 注文 | order.status_update | 注文ステータス変更 |
| 注文 | order.delete | 注文削除 |
| 顧客 | customer.view | 顧客閲覧 |
| テーマ | theme.view | テーマ閲覧 |
| テーマ | theme.create | テーマ作成 |
| テーマ | theme.edit | テーマ編集 |
| テーマ | theme.delete | テーマ削除 |

合計13 Permissionを基本とする。

---

## 10. 管理画面に「ユーザー・権限管理」を追加

管理画面に、

**設定 → ユーザー・権限管理**

を設ける。

ここからユーザーの追加・ロール確認・権限設定を行えるようにする。

画面イメージ：

```text
ユーザー・権限管理

┌────────────────────────────────┐
│ 山田太郎        スタッフ       [権限設定] │
│ 佐藤花子        マネージャー   [権限設定] │
└────────────────────────────────┘

[＋ ユーザーを追加]
```

---

## 11. ユーザーごとの権限設定画面

「権限設定」をクリックすると、ユーザーごとのPermissionを設定できる画面を表示する。

例：

```text
ユーザー：山田太郎
ロール：スタッフ

────────────────────

【商品管理】

☑ 商品を見る
☑ 商品を追加
☑ 商品を編集
☑ 商品画像を変更
☐ 商品を削除

【注文管理】

☑ 注文を見る
☑ 注文ステータスを変更
☐ 注文を削除

【顧客管理】

☑ 顧客を見る

【テーマ・サイト管理】

☐ テーマを見る
☐ テーマを作成
☐ テーマを編集
☐ テーマを削除

────────────────────

[すべてON] [すべてOFF] [保存]
```

**画面を見ただけで現在の権限状態が分かるUI**とする。

---

## 12. 権限設定UIの仕様

権限はチェックボックスまたはトグルスイッチでON/OFFできるようにする。

カテゴリごとにまとめる。

### 商品管理

- 商品を見る
- 商品を追加
- 商品を編集
- 商品画像を変更
- 商品を削除

### 注文管理

- 注文を見る
- 注文ステータスを変更
- 注文を削除

### 顧客管理

- 顧客を見る

### テーマ・サイト管理

- テーマを見る
- テーマを作成
- テーマを編集
- テーマを削除

---

## 13. 一括操作

権限設定画面には可能であれば、

- すべてON
- すべてOFF
- カテゴリ単位でON/OFF

を用意する。

例えば、

「商品管理をすべて許可」

「注文管理は閲覧だけ許可」

などを簡単に設定できるようにする。

---

## 14. 管理者の表示

管理者の場合は、個別Permissionのチェックボックスを変更できないようにする。

画面上では、

```text
ユーザー：管理者
ロール：管理者

すべての権限が有効です。
```

などと表示する。

管理者は常に全権限を持つ。

---

## 15. FastAPI側での権限チェック

権限判定は**FastAPIを最終的な判定箇所**とする。

Next.js側でボタンを非表示にするだけでは不十分。

例えば商品編集の場合、

```text
Next.js
   ↓
商品編集API
   ↓
FastAPI
   ↓
認証確認
   ↓
ユーザー確認
   ↓
adminか？
   ↓
YES → 許可
NO → product.editを確認
   ↓
権限あり → 許可
権限なし → 403 Forbidden
```

とする。

APIを直接呼び出された場合でも、権限のない操作は実行できないようにする。

---

## 16. Next.js側のUI制御

Next.js側ではユーザーのPermissionに応じてUIを制御する。

例えば、

```text
product.delete = false
```

の場合、

- 商品削除ボタンを表示しない
- 削除操作への導線を表示しない

などとする。

ただし、Next.js側のUI制御はあくまでユーザーインターフェース上の制御。

**セキュリティ上の権限チェックは必ずFastAPI側でも行う。**

---

## 17. データ構造

将来の拡張を考慮し、

```text
User
 ├─ id
 ├─ name
 ├─ email
 ├─ role
 └─ permissions
```

という考え方で設計する。

Permissionは固定的なコードとして管理する。

例：

```text
product.view
product.create
product.edit
product.image
product.delete

order.view
order.status_update
order.delete

customer.view

theme.view
theme.create
theme.edit
theme.delete
```

ユーザーごとのPermissionを保存できる構造とする。

---

## 18. 権限変更の対象

基本的には、

**管理者がマネージャー・スタッフの権限を変更する**

構造とする。

一般スタッフが自分自身の権限を変更できないようにする。

また、マネージャーについても、管理者の権限を変更できないようにする。

---

## 19. 初期権限

### 管理者

全権限。

### マネージャー

商品・注文・顧客等、店舗運営に必要な権限を広く許可。

### スタッフ

商品閲覧、商品登録、商品編集、注文閲覧、注文ステータス変更など、日常業務に必要な権限を中心に設定。

ただし、初期設定は後から管理者が変更できるものとする。

---

## 20. 一人運営について

現在は一人運営であっても、今回の権限システムは実装する。

ただし、一人運営時に複雑な設定を要求しない。

管理者アカウントを使用すれば、基本的にはすべての機能を利用できる状態とする。

将来的にスタッフを追加した場合に、

```text
スタッフA
→ 商品登録・編集OK
→ 削除NG

スタッフB
→ 注文処理OK
→ 商品編集NG

マネージャー
→ 商品・注文・顧客管理OK
→ テーマ変更OK
```

のように設定できることを目的とする。

---

## 21. 今回の設計思想

今回の権限管理は、

**「複雑な企業向け権限システムを作る」ことが目的ではない。**

イヤリング・アクセサリー・小物を販売するECサイトの運営に必要な範囲で、

**誰が何をできるのかを管理者が画面上で簡単に設定できる仕組み**

を作ることが目的。

そのため、

- 必要以上にPermissionを細分化しない
- 現在存在しない機能の権限は作らない
- 将来の機能追加には対応できる構造にする
- UIは分かりやすくする
- セキュリティ上の判定はFastAPIで行う

という方針とする。

---

## 22. 完成イメージ

最終的には管理画面の、

**設定 → ユーザー・権限管理**

から、

1. ユーザーを追加
2. ロールを設定
3. 権限設定画面を開く
4. 必要な権限をON/OFF
5. 保存

という流れで設定できるようにする。

管理者は全権限固定。

マネージャー・スタッフは個別権限設定可能。

この構成を基本仕様として実装する。
---

## 23. 承認フローについて

承認フローは、今回の権限管理とは切り分け、**別工程として実装する**。

現時点では、マネージャー・スタッフが権限のない操作を行った場合に、自動的に「承認申請」へ変換する仕組みは実装しない。

まずは、**権限の粒度とロールの位置づけを確定し、権限チェック機能を完成させることを優先する。**

### 23-1. 現時点で確定する範囲

- 権限はPermission単位で管理する。
- 管理者は全権限を持つ。
- マネージャー・スタッフは個別Permissionを設定できる。
- 権限のない操作はFastAPI側で拒否する。
- 権限のない操作を自動的に承認申請へ変換する機能は、現工程では実装しない。

### 23-2. 承認フローは別工程

将来的に承認フローを追加する場合は、以下を別途仕様確定した上で実装する。

#### 承認対象

すべての権限不足操作を承認申請にするのではなく、**承認が必要な操作を個別に指定できる設計**を基本方針とする。

例えば、

- 注文のキャンセル・削除
- 商品の削除
- 重要なサイト設定の変更

など、影響の大きい操作だけを承認対象とすることを想定する。

実際の対象操作は、承認フロー実装時に確定する。

#### 申請中データの扱い

例えば商品追加を承認対象とする場合、申請中の商品をどの状態で保存するかを別途決定する。

候補：

- 下書きとして保存し、承認後に公開する
- 申請専用データとして保存する
- 通常の商品データとして保存し、公開状態だけ制限する

ECサイトでは、承認前の商品が一般公開されないよう、**下書き・非公開状態で保持する方式を基本候補とする。**

#### 却下時の処理

却下された申請については、

- 却下理由を保存する
- 申請者が画面上で結果を確認できるようにする
- 必要に応じて修正して再申請できるようにする

ことを基本候補とする。

通知については、まず管理画面上での通知・申請履歴確認を基本とし、メール通知は必要性を確認した上で追加する。

#### 承認者

現時点の方針では、**承認できるのは管理者のみ**とする。

マネージャーは他のスタッフの申請を承認できない。

将来的にマネージャー承認を許可する必要が出た場合は、別途「承認者権限」を設計する。

### 23-3. 承認フロー実装時に必要となる機能

承認フローを実装する場合は、権限チェックとは別に以下を追加する。

- 承認申請テーブル
- 申請者・対象操作・対象データの記録
- 申請理由・申請日時の記録
- 承認・却下・保留等のステータス
- 承認者・承認日時の記録
- 却下理由の記録
- 承認／却下UI
- 申請履歴UI
- 申請者への結果通知
- 必要に応じたメール通知

したがって、承認フローは単純なPermissionチェックより一段階大きな機能として扱う。

---

## 24. 注文データの削除について

注文データは決済・売上・会計・税務等に関係する重要なデータであるため、**通常のDELETEによる物理削除は原則として行わない。**

`order.delete`を設ける場合でも、実際には、

- キャンセル
- 無効化
- アーカイブ

などの論理的な状態変更を基本とする。

どうしても物理削除が必要なケースがある場合は、管理者のみ実行可能とすることを基本方針とする。

「注文削除」というUI名称を使用する場合も、実際のデータベース上のDELETEとは区別する。

---

## 25. 現在のデータモデルからの変更

現在のデータモデルでは、`customers.is_admin` による単純な管理者／一般ユーザーのON/OFF判定となっている。

今回の仕様では、これを、

- role
- permissions

による構造へ拡張する。

そのため、実装時には**データベーススキーマの変更およびマイグレーションが必要**となる。

既存ユーザーの権限が失われたり、ログインできなくなったりしないよう、既存の`is_admin`ユーザーを新しい`admin`ロールへ移行する等、安全な移行手順を設ける。

---

## 26. 実装工程

今回の機能は、以下の2段階に分けて実装する。

### 第1工程：権限管理

まず以下を完成させる。

1. roleの導入
2. Permissionの導入
3. ユーザーごとのPermission保存
4. 管理者／マネージャー／スタッフのロール管理
5. 管理画面での権限設定UI
6. FastAPIでの権限チェック
7. Next.jsでのUI制御
8. 既存`is_admin`からのデータ移行

### 第2工程：承認フロー

第1工程が安定した後、必要性を確認して別工程として実装する。

その際、以下を改めて仕様確定する。

1. どの操作を承認対象にするか
2. 申請中データをどう保持するか
3. 承認・却下後のデータ状態
4. 却下理由の扱い
5. 申請者への通知方法
6. 承認者を管理者だけにするか、マネージャーにも許可するか
7. 申請履歴をどこまで保持するか
8. 必要なテーブル・API・UI

この2段階に分けることで、まず基本的な権限管理を確実に実装し、承認フローによる不要な複雑化を避ける。

---

## 27. 最終方針

現時点では、**権限管理と承認フローを同時に実装しない。**

まず、

**「誰が何をできるか」＝ Permission**

を管理画面から明確に設定できる仕組みを完成させる。

その上で、実際の運用を見ながら、

**「誰かの承認が必要な操作は何か」＝ 承認フロー**

を別途設計する。

これを今回の基本方針とする。

---

## 28. 実装時の申し送り事項（次工程で確定すること）

第1工程（権限管理）に着手する際、現在の実装（FastAPI + Next.js、`customers`テーブル）と本仕様を突き合わせて、以下5点を先に確定してから着手する。

### 28-1. 「customer」ロールが未定義

ロールはadmin/manager/staffの3種類が定義されているが、**買い物をするだけの一般会員用のロールが無い**。

`customers.is_admin`を`role`列に置き換える際、既存の`is_admin=false`ユーザー（一般顧客）全員を割り当てる4つ目のロールとして`role="customer"`を追加する。

- `is_admin=true` → `role="admin"`
- `is_admin=false` → `role="customer"`

### 28-2. `theme.view`はAPIレベルでは権限チェックしない

現在のテーマ一覧API（`GET /api/themes`）は、管理画面だけでなく**店頭表示（ショップページ）でも使う公開API**である。

`theme.view`をFastAPI側で強制すると店頭表示まで権限で塞いでしまうため、以下の方針とする。

- 閲覧（`theme.view`）：APIでは権限チェックしない（誰でも読める公開情報のまま）。管理画面のナビ表示/非表示の制御にのみ使う。
- 作成・編集・削除（`theme.create` / `theme.edit` / `theme.delete`）：FastAPI側で権限チェックする。

### 28-3. 注文の論理削除は`status`と分離する

`orders.status`には既に`"cancelled"`（顧客都合等の通常キャンセル）が存在する。

「スタッフが誤って作ったテスト注文を一覧から除外したい」場合と、通常の顧客都合キャンセルを同じ`status="cancelled"`で扱うと意味が混ざる。

そのため、`orders`テーブルに**`deleted_at`（論理削除用タイムスタンプ、nullable）を追加**し、`status`とは独立させる。

- `order.delete`（論理削除）：`deleted_at`に日時をセットし、一覧・検索から除外する。`status`は変更しない。
- 物理DELETE：管理者のみ実行可能な別操作として残す（通常のUIフローには出さない）。

### 28-4. 権限の保存形式

`customers`テーブルに以下を追加する。

- `role`列（enum: `admin` / `manager` / `staff` / `customer`）
- `permissions`列（JSON配列。許可されたpermission codeのリスト。例: `["product.view", "product.edit"]`）

別テーブルに正規化するより単純で、「必要以上に複雑にしない」という本ドキュメントの方針（21章）にも合致するため、この形式を採用する。

`role="admin"`の場合、`permissions`の中身に関わらず常に全権限を許可する（14章の通り）。

### 28-5. 管理者向け「顧客一覧」からスタッフ/マネージャーを除外する

マネージャー・スタッフも同じ`customers`テーブルに入る設計のため、既存の「顧客一覧」画面（`/admin/customers`）にスタッフ・マネージャー・管理者のアカウントが混ざらないよう、一覧取得時に`role="customer"`だけへ絞り込むフィルタを追加する。

