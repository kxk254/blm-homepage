# docker-compose用。このファイルをコピーして`.env`として置く(docker composeが自動で読む)。

# NASの共有フォルダをホストOS側で事前にマウントしておいたパス(絶対パス推奨)。
# 未設定の場合はリポジトリ直下の./data/mediaにフォールバックする(ローカル動作確認用)
NAS_MEDIA_PATH=/mnt/synology/blm-images
